package a

var good = "—"
var goodAscii = "plain ascii"
var goodRaw = `\u2014`
var goodEmpty = ""

var bad = "—"              // want `Use the actual character instead of a \\uXXXX escape sequence.`
var badUpper = "—" // want `Use the actual character instead of a \\uXXXX escape sequence.`
var badChar = '—'          // want `Use the actual character instead of a \\uXXXX escape sequence.`

// Unfixable: control character escape stays as-is (diagnostic but no fix)
var unfixableControl = "\u0007" // want `Use the actual character instead of a \\uXXXX escape sequence.`

// Unfixable: double quote inside double-quoted string
var unfixableQuote = "\u0022" // want `Use the actual character instead of a \\uXXXX escape sequence.`

// Unfixable: mixed safe and unsafe in one literal (all-or-nothing)
var unfixableMixed = "\u2014\u0007" // want `Use the actual character instead of a \\uXXXX escape sequence.`

// Fixable: multiple safe escapes in one literal
var fixableMulti = "—–" // want `Use the actual character instead of a \\uXXXX escape sequence.`
